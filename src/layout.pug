include ./blocks/head-foot/header
include ./blocks/head-foot/footer
include ./blocks/form/elements
include ./blocks/cards/card-blocks

doctype html
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        block title
    body
        block header
            +header(false)
        block content
        block footer
            +footer(true)